  function setup(){
    createCanvas(600, 600);
    background("white")
  }
function draw() {
  stroke("yellow");
  fill("green");
  
  
  if (mouseIsPressed) {
    rect(mouseX, mouseY, 25, 25)
  }
}
